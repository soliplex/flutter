// TODO(tui): Implement tool approval modal dialog.
// Currently all tools are auto-approved. When nocterm overlay support
// is fully explored, replace this with an interactive approval dialog
// that lets users accept/reject tool calls before execution.
