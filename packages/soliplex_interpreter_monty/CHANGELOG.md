# Changelog

## 0.3.0

- Replaced 12 duplicated bridge source files with `dart_monty_bridge` dependency (#141).
- Barrel export re-exports all bridge types via strict `show` combinator.

## 0.2.0

- Added `MontyPlugin` interface for host function plugin contracts (#76).

## 0.1.0

- Initial release.
